# RAG 专题

## ⏳ 待整理清单

| # | 题号 | 内容 | 状态 | 解决节点 |
|---|------|------|:---:|---------|
| 1 | 题14 | 整题空壳（目前只写了"参考题13"） | 🔴 | 阶段 2.7（评估实验后独立撰写） |
| 2 | 题12 Q2 | bge-m3 维度对比实验 | 🟡 | 阶段 2.7 |
| 3 | 题19 Q2 | ANN vs 暴力搜索延迟对比数据 | 🟡 | 阶段 2.7 |
| 4 | 题3 Q2 | 线性加权 vs RRF 定量对比 | 🟡 | 阶段 2.7 |
| 5 | 题3 Q3 | α 值对比实验数据 | 🟡 | 阶段 2.7 |
| 6 | 题52 Q2 | 不同候选数量的检索命中率对比 | 🟡 | 阶段 2.7 |
| 7 | 题52 Q5 | 不同归一化方法效果验证 | 🟡 | 阶段 2.7 |
| 8 | 题19 Q4 | HNSW vs IVF 深入对比 | 🟡 | 阶段 2.2（向量数据库对比） |
| 9 | 题13 Q3 | bge-m3 高并发部署方案 | 🟡 | 阶段 4.5（vLLM/SGLang） |
| 10 | 题3 Q4 | 百万级文档混合检索优化 | 🟡 | 阶段 4.5（性能压测） |
| 11 | 题52 Q4 | 混合检索延迟定量数据 | 🟡 | 阶段 4.5（性能压测） |
| 12 | 题24 | 召回与意图不匹配的改进 | 🔴 | 意图路由迁移完成后 |
| 13 | 题27 | RAG 幻觉问题的实用解决方法 | 🔴 | 三级降级策略完善后 |
| 14 | 题40 | 生产环境性能优化与成本控制 | 🔴 | 阶段 4（工程化学习后） |
| 15 | 题44 | 检索不到文档的处理方案 | 🔴 | 阈值检测+降级策略落地后 |

> 📊 共 15 项：🟡 10 项（6 项 → 阶段 2.7 本周五集中消灭）+ 🔴 4 项

---

## 题12：解读 RAG 中的 Embedding 嵌入技术

**我的回答**：

Embedding（嵌入）是一种将高维、离散、非结构化数据映射到**低维连续向量空间**的技术。它解决了传统 one-hot 编码维度爆炸、语义丢失、无法计算相似度的核心痛点，具有低维稠密、保留语义、高效检索的特点。

在我项目里，我使用 bge-m3 模型将用户问题和知识库文档都转化为 1024 维向量，并存入 ChromaDB。当用户提问时，系统会将问题向量化，然后通过余弦相似度在 ChromaDB 中快速找到语义最接近的文档片段，最终将这些信息作为上下文注入 LLM，有效抑制了幻觉，提升了回答的专业性。

**项目关联**：
- 在 baby-ai 项目中，`get_embedding` 函数调用 bge-m3 模型，实时将文本转化为 1024 维语义向量。
- 核心实践：用户提问 -> Embedding -> ChromaDB 余弦相似度匹配 -> 返回最相关文档 -> 注入 LLM。选择 bge-m3 是因为其对中文友好、支持 8192 tokens 长文本，且同时具备 Dense 和 Sparse 检索能力，为后续升级混合检索留足了空间。

**关键词**：
Embedding（嵌入）、one-hot、余弦相似度、语义匹配、bge-m3、ChromaDB

---

**面试官可能追问**：

**Q1：为什么用余弦相似度而不是欧氏距离？**
🟢 当前状态：能讲清
- 在高维语义空间中，方向比绝对距离更能代表语义，余弦相似度只关注夹角，消除了文本长度对向量模的影响。
- 主流向量数据库（如 ChromaDB）对余弦相似度有底层优化，检索效率更高。
- 我可以举例：一句长回答和一句短总结，虽然向量长度不同，但语义相近时余弦值依然接近 1。

**Q2：bge-m3 的 1024 维是怎么定的？维度越高越好吗？**
🟡 当前状态：概念可讲，未做定量对比 → 📅 阶段 2.7（评估脚本中可补充不同维度模型的延迟与召回率对比实验）
- 不是越高越好，这是精度与性能的经典工程权衡。
- 维度越高，语义编码越精细，但计算量、存储成本和检索延迟也越大。
- 1024 维是平衡点：既能捕获足够语义特征，又能在 CPU 环境毫秒级响应；相比 OpenAI 1536 维节省约 1/3 存储。
- 📅 计划：若面试岗位对性能要求极高，可补做不同维度模型的延迟与召回率对比实验。

**Q3：bge-m3 和 text-embedding-3-small（OpenAI）你具体是怎么选的？**
🟢 当前状态：能讲清
- 从四个维度综合考量：
  1. 功能完整性：bge-m3 自带 Sparse（稀疏关键词） 检索，一个模型实现混合检索；OpenAI 仅 Dense。
  2. 成本与隐私：bge-m3 开源本地部署，零调用费、零数据出境风险；OpenAI 按 token 计费。
  3. 性能：本地无网络延迟，适合育儿问答的低延迟要求。
  4. 中文优化：bge-m3 在中文检索基准（如 T2Retrieval）上表现优秀。
- 结论：bge-m3 更适合我对功能、成本、隐私的综合要求。

**Q4：在你的项目里，如果不用 Embedding，会有什么后果？**
🟢 当前状态：能讲清
- RAG 管道基本失效。用户问“宝宝发烧”，知识库写的是“小儿发热护理”，传统关键词匹配会因用词不同直接漏掉正确答案。
- 只有 Embedding 能将“发烧”和“发热”映射到相近向量，实现语义理解，从而精准召回知识。
- 这体现了 Embedding 在非结构化文本检索中的不可替代性。


## 题13：RAG 中常用的 Embedding Model 嵌入模型

**我的回答**：

目前主流的中文 Embedding 模型有三类，我在项目里选的是 **bge-m3**，核心原因是它在功能完整性和中文检索上最匹配我的需求。

**先看对比**：

| 模型 | 维度 | 中文检索 | 功能特点 | 部署方式 | 问题 |
|------|------|:---:|------|---------|------|
| m3e-base | 768 | 中等 | 仅 Dense，512 tokens | 本地 | 轻量但功能单一，长文本需大量分块 |
| text-embedding-3-small | 1536 | 良好 | 仅 Dense | OpenAI API | 方便但收费，有网络延迟 |
| **bge-m3** | 1024 | **优秀** | Dense + Sparse + Multi-vector，8192 tokens | Ollama 本地 | ✅ 功能最全 |

**我的选型逻辑**：

m3e-base 虽然轻量、中文综合排名不低，但它只支持 Dense 检索，要想实现混合检索还得额外部署 BM25 引擎，架构复杂度翻倍。text-embedding-3-small 方便，但按 token 计费、有网络延迟，而且数据要出境。

bge-m3 解决了这两个问题：第一，它自带 Sparse 检索，生成 Dense 向量的同时免费产出 token 权重，一个模型就能实现官方推荐的 Hybrid Retrieval，架构最简；第二，开源本地部署，零调用费、零延迟；第三，支持 8192 tokens 长文本输入，处理育儿知识库里的长文章不用强制截断。再加上它在 MTEB 中文检索任务上表现优秀，所以选它是顺理成章的事。

**项目关联**：
- baby-ai 中 `get_embedding` 函数调的就是 bge-m3，所有用户问题和知识库文档都通过它实时转为 1024 维向量，存入 ChromaDB。
- 检索流程：用户提问 → Embedding → ChromaDB 余弦相似度匹配 → 返回最相关文档 → 注入 LLM。

**关键词**：
Embedding 模型选型、bge-m3、m3e-base、text-embedding-3-small、混合检索、MTEB

---

**面试官可能追问**：

**Q1：m3e-base 中文综合排名 67，bge-m3 才 91，你为什么说 bge-m3 更好？**
🟢 当前状态：能讲清

> “MTEB 中文综合排名包含了分类、聚类等和检索不直接相关的子任务，m3e-base 在分类聚类上表现好，所以总榜高。但对我项目真正重要的是 Retrieval 子任务的 NDCG 指标，bge-m3 在这方面是优秀的。而且功能维度上，bge-m3 多出 Sparse 检索和 8192 tokens 长文本支持，这不是排名数字能体现的。选型不是单看排名，而是看功能匹配度。”

**Q2：text-embedding-3-small 是 OpenAI 的模型，知名度更高，你为什么不用？**
🟢 当前状态：能讲清

> “从四个维度考虑：①功能——bge-m3 支持 Dense+Sparse，OpenAI 只支持 Dense；②成本——bge-m3 免费，OpenAI 按 token 收费；③隐私——本地部署数据不出境；④延迟——本地调用无网络开销。综合下来 bge-m3 更匹配我对功能、成本、隐私的要求。”

**Q3：如果后续业务量激增，你还会继续用 bge-m3 吗？**
🟡 当前状态：概念可讲，待实践 → 📅 阶段 4.5（vLLM/SGLang 高并发部署）

> “会继续用，但部署策略会调整。bge-m3 本身支持 Dense 和 Sparse 两种检索，核心能力不会换。如果单机 Ollama 扛不住高并发，我会考虑用 vLLM 或 SGLang 部署 bge-m3，利用 PagedAttention 和连续批处理提升吞吐量。”

**Q4：你提到 bge-m3 支持 Dense、Sparse、Multi-vector，能简单解释一下这三者的区别吗？**
🟢 当前状态：能讲清

> - **Dense（稠密向量检索）**：把整段文本压缩成一个固定维度的连续向量（如 1024 维），用余弦相似度做语义匹配。优点是能理解同义表达（“发烧”≈“发热”），缺点是可能忽略精确关键词。典型场景：语义搜索、模糊问答。
> - **Sparse（稀疏关键词检索）**：类似 BM25，把文本表示成高维稀疏向量，每个词有独立的权重分数。优点是精确匹配能力强，专有名词（如“对乙酰氨基酚”）不会漏；缺点是无法理解语义变体。典型场景：专有名词检索、精确匹配。
> - **Multi-vector（多向量检索）**：把文本中每个 token 都生成一个向量，用多个向量的组合来表示整段文本。优点是细粒度最高，能捕捉词与词之间的细微关系；缺点是计算量和存储成本显著增大。典型场景：需要精细语义匹配的专业检索。
>
> **我的使用策略**：目前主要用 Dense 做语义召回，后续会加上 Sparse 做精确匹配，两者组合成官方推荐的 Hybrid Retrieval。Multi-vector 暂时未落地，因为对硬件和索引结构要求较高，适合在知识库规模扩大、检索精度要求更高的阶段再引入。


## 题14：RAG 中 Embedding Model 的选型因素及方法

参考题13的回答 

## 题19：向量数据库的核心原理简述

**我的回答**：

向量数据库的核心原理是：**将非结构化数据通过 Embedding 模型转成向量，用近似最近邻算法做高效相似度检索。** 传统数据库做精确匹配，向量数据库做语义相似度匹配--把用户问题和知识库文档都转成向量，用余弦相似度或内积计算距离，找到最接近的 top-K 结果。

为了在海量向量中快速检索，向量数据库不会逐个比对（那样太慢），而是用 ANN 算法做近似匹配。常见的索引结构有 HNSW（分层可导航小世界图）、IVF（倒排索引）等，能在毫秒级返回结果。

我项目里用的 ChromaDB，就是基于 HNSW 索引的轻量级向量数据库。用户提问后，bge-m3 生成 1024 维向量，ChromaDB 用余弦相似度检索最相关的文档片段。

**项目关联**：
- baby-ai 使用 ChromaDB 存储向量，支持双集合并行检索（rag_samples + rag_medical）。
- 检索流程：用户提问 → bge-m3 向量化 → ChromaDB 余弦相似度搜索 → 返回 top-K → 注入 LLM。

**关键词**：向量数据库、近似最近邻算法、余弦相似度、HNSW、ChromaDB

---

**面试官可能追问**：

**Q1：向量数据库和传统数据库的核心区别是什么？**

🟢 当前状态：能讲清

> “传统数据库做精确匹配，只能返回完全一致的结果。向量数据库做语义相似度匹配，能理解同义表达。它的索引结构专门为高维向量的相似度搜索做了优化。”

**Q2：ANN 近似最近邻和暴力搜索有什么区别？为什么用 ANN？**

🟡 当前状态：概念可讲，未做定量对比 → 📅 阶段 2.7（评估脚本中可补充暴力搜索 vs ANN 的延迟对比数据）

> "暴力搜索是逐个计算查询向量与所有文档向量的距离，精度 100%，但百万级数据下延迟不可接受。ANN 牺牲微小的精度（如 95% 召回率），换取了毫秒级的检索速度。常用算法有 HNSW（基于图的多层索引，ChromaDB 使用）和 IVF（基于聚类的倒排索引）。"

**Q3：你项目中 ChromaDB 选型的原因？有没有考虑过 Milvus 或 Pinecone？**

🟢 当前状态：能讲清

> “ChromaDB 轻量、可嵌入式部署，和我的本地开发环境完美匹配。Milvus 适合企业级分布式场景，Pinecone 是全托管云服务。目前阶段 ChromaDB 足够用，后续规模扩大再考虑迁移。”

**Q4：HNSW 和 IVF 这两种索引结构的区别是什么？ChromaDB 为什么选 HNSW？**

🟡 当前状态：能讲清概念

>"HNSW 是基于图的多层索引，构建多层导航网络，检索时从顶层快速定位再逐层下沉，适合内存内的高性能检索。IVF 是基于聚类的倒排索引，先把向量分成若干簇，检索时只搜最近的几个簇。ChromaDB 选 HNSW 是因为它作为嵌入式数据库，通常在单机内存环境下运行，HNSW 在内存内的检索速度优于 IVF，且不需要额外的聚类训练步骤。"


## 题3：混合检索的定义及在大模型应用中的核心作用

**我的回答**

**定义**：
混合检索是将 **Dense（稠密向量检索）** 和 **Sparse（稀疏关键词检索）** 结合到一起的检索方案。Dense 擅长理解同义表达（“发烧”≈“发热”），Sparse 擅长精确匹配专有名词（“对乙酰氨基酚”）。两者互补，解决了纯 Dense 或纯 Sparse 各自召回不全的问题。

**核心作用**：
在大模型应用中，混合检索的核心作用是**从源头减少因信息缺失而导致的 LLM 幻觉**。传统关键词匹配无法理解语义变体，纯向量检索又容易漏掉精确的关键词——比如用户问“对乙酰氨基酚怎么吃”，纯 Dense 可能只召回“退烧药”相关文档，不包含精确剂量信息。Sparse 则能精准命中“对乙酰氨基酚”这个词，召回包含剂量表的文档。两者结合后，LLM 既有护理指南又有精确剂量，回答既全面又准确。

**项目关联**：
在我的育儿助手项目里，我用 bge-m3 一个模型同时输出 Dense 和 Sparse 两种向量，省去了额外部署 BM25 引擎的成本。融合策略上，我先用线性加权（α=0.7）快速验证效果，后续切换到 RRF 以获得更鲁棒的融合结果。实测中，对于“宝宝发烧对乙酰氨基酚怎么吃”这样的复合查询，混合检索能将语义相关的护理指南和精确匹配的剂量表同时返回，显著提升了检索命中率。

**关键词**：
混合检索、Hybrid Retrieval、Dense、Sparse、bge-m3、RRF、线性加权

---

**面试官可能追问**：

**Q1：混合检索和单纯的 Dense 检索相比，在你的项目里具体提升了什么？**

🟢 当前状态：能讲清

>"提升主要体现在对精确关键词的召回上，比如用户问”对乙酰氨基酚怎么吃“，纯 Dense 检索可能只能召回”退烧药“相关的泛化文档，漏掉了包含精确剂量信息的文档。加了Saprse后，‘对乙酰氨基酚’这个词被精确匹配到，剂量表被成功召回，从我的实验数据看，混合检索让这类复合查询的命中率从部分召回提升到了完整召回，LLM既能讲清互利原则，又能给出精确剂量，幻觉明显减少。"


**Q2：你在项目里同时实现了线性加权和 RRF，实际效果上有什么区别？**

🟡 当前状态：概念可讲，待做定量对比 → 📅 阶段 2.7（用同一批测试问题对比两种融合策略）

> “线性加权的优点是直观，能通过 α 灵活控制语义和关键词的相对重要性。但问题是 α 需要手动调，而且 Dense 和 Sparse 的分数量纲不同，必须归一化后才能相加。RRF 只关心排名，天然绕过了量纲问题，也不需要调参。从理论上讲，RRF 对噪声更鲁棒——如果一篇文档 Dense 排名中上但 Sparse 排名极低，它的 RRF 分数会被自然压低，不容易混进 top-K。我项目里先用线性加权快速验证可行性，后续切换到 RRF 作为默认融合策略。如果有机会做定量对比，我会用同一批测试问题，统计两种策略的检索命中率和 LLM 回答准确率。”

**Q3：你的 α 参数（0.7）是怎么确定的？有没有做过对比实验？**

🟡 当前状态：概念可讲，实验数据待补充 → 📅 阶段 2.7（用不同 α 值做检索命中率对比实验）

> “0.7 是我基于育儿场景的经验预设——育儿问答中，语义泛化通常比精确关键词更重要，所以给 Dense 更高的权重。但我目前只做了小规模的手动验证，还没系统性地做对比实验。如果要严谨确定 α，我会拿 20-30 个测试问题，用 0.3/0.5/0.7/0.9 四个 α 值各跑一遍，统计检索命中率和最终回答的准确性，选出综合表现最好的值。这也是我后续评估体系搭建时会做的事情。”


**Q4：混合检索增加了 Sparse 的计算开销，如果生产环境中知识库有百万级文档，你怎么优化？**

🟡 当前状态：概念可讲，待实践

> “确实有额外开销。优化方向有几个：第一，BM25 的索引可以在文档入库时离线构建，查询时只需计算查询词和已有索引的匹配，延迟很低；第二，可以先用 Sparse 做粗筛（快速过滤掉完全不相关的文档），再对候选集做 Dense 精排，而不是两路各取 top-K 再融合；第三，如果规模再扩大，可以用 Elasticsearch 或 Milvus 的混合检索插件，它们内部已经做了工程优化。bge-m3 的一个优势是生成 Dense 向量时免费附带 Sparse token weights，省去了额外部署 BM25 引擎的成本，在中等规模下已经够用。”

## 题52：混合检索的定义及向量检索与关键词检索的结合实现

**我的回答**

**定义**：
混合检索是将 Dense（稠密向量检索，管语义）和 Sparse（稀疏关键词检索，管精确）结合到一起的检索方案。它解决了纯 Dense 对专有名词不敏感、纯 Sparse 无法理解同义表达的问题，两者互补，显著提升召回完整性。

**实现方式**：
目前主流的融合策略有两种，我在项目里都落地了：

**1. 线性加权**
核心思想是给 Dense 和 Sparse 各一个权重，加权求和：`最终分数 = α × Dense分数 + (1-α) × Sparse分数`。
- **融合步骤**：① 两路各取 top-K 候选 → ② Min-Max 归一化把两路分数统一到 0~1 → ③ 加权求和 → ④ 按最终分数重新排序。
- **α 的作用**：α 越接近 1 越偏重语义，越接近 0 越偏重关键词。育儿场景下语义泛化通常更重要，我默认设 0.7。
- **关键坑点**：必须归一化！Dense 的余弦相似度是 0~1，Sparse 的 BM25 分数无上限，不归一化会导致 Sparse 分数碾压 Dense，α 形同虚设。

**2. RRF（倒数排名融合）**
核心思想是不看具体分数，只看排名位置：`RRF分数 = 1/(60 + Dense排名) + 1/(60 + Sparse排名)`。
- **为什么要用 RRF**：线性加权的最大问题是两路分数量纲不同，必须归一化才能相加。RRF 只关心排名，天然绕过了量纲问题，也不需要手动调 α。
- **效果**：如果一篇文档 Dense 排名第 1 但 Sparse 排名第 20，线性加权可能仍把它排得很高（Dense 分数高），而 RRF 会因为 Sparse 排名差而压低它。RRF 奖励的是“两路都排得靠前”的文档，对噪声更鲁棒。

**项目关联**：
在我的育儿助手项目里，我用 bge-m3 一个模型同时输出 Dense 和 Sparse 两路向量，省去了额外部署 BM25 引擎的成本。融合策略上，我先用线性加权（α=0.7）快速验证混合检索的可行性，后续切换到 RRF 以获得更鲁棒的融合效果，无需手动调参。实测中，对于“宝宝发烧对乙酰氨基酚怎么吃”这样的复合查询，混合检索能将语义相关的护理指南和精确匹配的剂量表同时召回，显著提升了检索命中率。

**关键词**：
混合检索、Hybrid Retrieval、Dense、Sparse、线性加权、归一化、RRF、bge-m3

---

**面试官可能追问**

**Q1：bge-m3 输出 Sparse 向量是怎么做到的？和传统 BM25 有什么区别？**

>"bge-m3 生成 Dense 向量的同时，会对每个 token 输出一个权重分数，这些权重组合起来就是 Sparse 向量——本质上和 BM25 一样，都是给每个词算重要性权重。区别在于：传统 BM25 需要单独部署一个检索引擎，用 TF-IDF 公式手动计算词权；bge-m3 的 Sparse 权重是模型自己学出来的，不需要额外部署，且和 Dense 共享同一个编码过程，几乎零额外成本。这让我一个模型就能实现混合检索，架构简洁很多。"


*Q2：你的混合检索中，Dense 和 Sparse 各取多少条候选？为什么这样设计？**
🟡 当前状态：能讲清设计逻辑，待做定量对比 → 📅 阶段 2.7（对比不同候选数量的检索命中率）

> “目前两路各取 20 条候选，然后合并后筛选 top-5 返回。取 20 条是为了给融合阶段留足够的候选池——如果某一路只取 5 条，另一路的重要文档可能根本没进候选池，融合就无从谈起。具体数字是经验值，如果要精确确定，我会用不同候选数量（10/20/30）做对比实验，看检索命中率的边际收益。超过一定数量后，新增的候选大多是噪声，反而拖慢检索速度。”


**Q3：如果某篇文档在 Dense 里排前 3，但在 Sparse 里完全没匹配（分数为 0），你会怎么处理？**
🟢 当前状态：能讲清

> “这种情况说明这篇文档语义高度相关但不包含查询中的精确关键词。用 RRF 时，我会给缺失的那一路分配一个较大的排名值（如候选池大小 + 1），这样文档的 RRF 总分会被适当压低，但不会被完全排除。用线性加权时，缺失那一路的分数为 0，最终分数完全由另一路决定。两种方式都不会直接丢弃它，只是降权。如果业务上认为精确关键词必须命中，可以设一个 Sparse 分数的最低阈值，低于阈值就直接过滤——但这需要根据具体场景权衡。”

**Q4：混合检索增加了多少延迟？和生产环境的纯 Dense 检索相比，性能损耗有多大？**
🟡 当前状态：概念可讲，定量数据待测 → 📅 阶段 4.5（模型部署方案对比 + 性能压测）

> “Sparse 检索（BM25）的计算复杂度远低于 Dense 的向量相似度计算，所以额外延迟主要来自分词和分数融合，实际增量很小——在我的本地环境下，从纯 Dense 切换到混合检索，响应延迟几乎无感知变化。bge-m3 的一个优势是 Sparse 权重和 Dense 向量在同一次编码中产出，不需要额外调模型，比单独部署 BM25 引擎的方案快得多。如果要定量验证，我会用压测工具对比两种方案的 P50/P99 延迟。”

**Q5：你提到了 Min-Max 归一化，还有没有其他归一化方法？为什么选 Min-Max？**
🟡 当前状态：能讲清概念 → 📅 阶段 2.7（评估搭建时可顺带验证不同归一化方法的效果）

> “常见的还有 Z-score 标准化（减均值除标准差），它适合数据服从正态分布的场景。混合检索中的 Sparse 分数通常是长尾分布——少数文档分数很高，大部分集中在低分段。Min-Max 更适合这种分布，因为它直接映射到 0~1，不受分布形态影响。另外 Min-Max 保留了原始分数的相对大小关系，这正是线性加权需要的信息。如果用 RRF，归一化就完全不需要了。”


## 题24：RAG 系统召回结果与用户 query 意图不匹配的改进方向

🔴 待整理 → 📅 意图路由迁移完成后
- 已掌握：Query Rewrite、混合检索
- 待补：意图路由、Rerank 深入

**我的回答**

召回结果不匹配，可以从下面几个方向考虑：

1. **Query Rewrite(查询改写)*：处理用户输入中的代词、省略、模糊表达。比如用户在多轮对话中说”他应该吃什么药“，通过指代消解把”他“还原为”宝宝“，让检索更精确

2. **混合检索**：纯 Dense 对专有名词不敏感，纯 Sparse 无法理解语义变体。用 Dense + Sparse 混合检索，Dense 管语义泛化（”发烧“ ≈ ”发热“），Sparse 管精确匹配（对乙酰氨基酚），两者互补提升召回完整性。


**项目关联**：


**关键词**：
Query Rewrite、混合检索、召回优化


## 题27：RAG 系统幻觉问题的实用解决方法

🔴 待整理 → 📅 三级降级完善后
- 已掌握：混合检索、条件检查 Prompt
- 待补：全链路降级策略

**我的回答**

Rag的幻觉主要来源于检索环境的信息缺失或错误，我的理解分为：

1. **检索层**：用混合检索（ Dense  + Sparse）提升召回完整性 - Dense管语义泛化，Sparse管精确关键词，两者互补减少因信息缺失导致的幻觉。比如用户问”对乙酰氨基酚怎么吃“，混合检索同时召回护理指南和精确计量表，LLM不需要靠训练数据编造计量。

2. **prompt**:：在 Prompt 中要求LLM先判断参考资料是否包含信息，不包含就诚实说明，而不是硬编。同时用结构化输出约束回答格式，减少自由发挥的空间。

**项目关联**：
- baby-ai 项目落地了上述三层方案：混合检索 + 条件检查 Prompt + 三级降级策略。

**关键词**：
幻觉抑制、混合检索、条件检查、降级策略、结构化输出

## 题40：生产环境中 RAG 系统的性能优化与成本控制方法

🔴 待整理 → 📅 阶段 4 工程化学习后
- 已掌握：bge-m3 单模型双输出降本
- 待补：缓存策略、vLLM 部署、Token 优化

**我的回答**


## 题44：RAG 检索不到相关文档的处理方案

🔴 待整理 → 📅 阈值检测 + 降级策略落地后
- 已掌握：Query Rewrite、混合检索双路互补
- 待补：相似度阈值检测、兜底知识库

**我的回答**

检索不到相关文档的时候，不是返回”找不到“，而是需要有兜底策略：

1. **扩大检索范围**：使用**Query Rewrite** 对原始查询扩展，增加命中概率。

2. **混合检索的双路互补**：Dense 管语义泛化，Sparse 管精确关键词。两者互补提高召回率，降低”找不到”的概率。

<!-- 3. **降级为纯 LLM 回答**：当检索结果 -->

**项目关联**：
<!-- - baby-ai 实现了相似度阈值检测 + 降级为纯 LLM 回答的机制，并在 Prompt 中要求标注信息来源的可靠性。 -->

**关键词**：
<!-- 检索降级、兜底策略、Query Rewrite、混合检索、阈值检测 -->
Query Rewrite、混合检索